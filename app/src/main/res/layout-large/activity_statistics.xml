<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"> <!-- Increased padding for larger screens -->

    <!-- Header Section -->
    <LinearLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="8dp"> <!-- Additional padding -->

        <ImageView
            android:id="@+id/imageBackButton"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@drawable/free_icon_back_arrow_5637688"
            android:contentDescription="Назад" />

        <TextView
            android:id="@+id/statisticsTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Статистика циклов"
            android:textColor="@color/black"
            android:textSize="34sp"
            android:textStyle="bold" /> <!-- Increased margin -->
    </LinearLayout>

    <!-- Scrollable List Content -->
    <ListView
        android:id="@+id/cycleHistoryListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/headerLayout"
        android:layout_marginTop="12dp" /> <!-- Increased top margin -->
</RelativeLayout>
